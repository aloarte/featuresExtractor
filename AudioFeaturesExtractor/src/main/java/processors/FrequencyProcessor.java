package processors;

/**
 * Performs all the frequency features process
 */
public class FrequencyProcessor {

    /**
     * Process the audio source to retrieve a float value that represents its MFCCS
     * @param audioSource      Raw audio source in byte[] format
     * @return                 Float with the MFCCS value
     */
    public static float calculateMFCCS(final byte[] audioSource){


        return 0.04F;
    }
}
